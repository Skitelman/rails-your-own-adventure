<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= @story.title  %></title>
  </head>
  <body>
    <h1><%= @story.title  %></h1>

    <p>
      <%= @story.description %>
    </p>

    <h3><%= link_to "Begin #{@story.title}", story_chapter_path(@story, @story.chapters.first)%></h3>
    <%= link_to "Edit #{@story.title}", edit_story_path(@story)%>
    <br>
    <hr>
    <br>
    <%= link_to "All Stories", stories_path%>
    </body>
</html>
